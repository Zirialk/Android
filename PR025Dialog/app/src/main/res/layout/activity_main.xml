<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <Button
        android:id="@+id/btnPulsame"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pulsame"
        android:layout_centerInParent="true">

    </Button>
    <Button
        android:id="@+id/btnSiNo"
        android:text="Si/No"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/btnPulsame"
        android:layout_centerHorizontal="true"/>

    <Button
        android:id="@+id/btnEquipo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/btnPulsame"
        android:text="Equipo"/>
    <Button
        android:id="@+id/btnSeleccionSimple"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Seleccion simple"
        android:layout_below="@+id/btnSiNo"
        android:layout_centerHorizontal="true"/>
    <Button
        android:id="@+id/btnMultiple"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Seleccion Múltiple"
        android:layout_below="@id/btnSeleccionSimple"
        android:layout_centerHorizontal="true"/>
    <EditText
        android:id="@+id/btnLayoutPersonalizado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Con layout personalizado"
        android:layout_above="@id/btnEquipo"
        android:layout_centerHorizontal="true"/>
    <EditText
        android:id="@+id/txtDatePicker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="DatePicker"
        android:layout_above="@id/btnLayoutPersonalizado"
        android:layout_centerHorizontal="true"/>
</RelativeLayout>
